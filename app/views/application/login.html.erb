<h1>Welcome to Wonky Veg login</h1>
<h2>Are you customer or a farmer?</h2>

<div>
  <h3>Customer</h3>
  <%= form_with model: @user, url: sessions_path, method: :post, local: true do |f| %>
    <%= render partial: 'user_login_form', locals: {f: f} %>
  <% end %>
  <br>
  <%= button_to "Login with Google", google_login_path %>
</div>

<div>
  <h3>Farmer</h3>
  <%= form_with model: @farmer, url: sessions_path, method: :post, local: true do |f| %>
    <%= render partial: 'farmer_login_form', locals: {f: f} %>
  <% end %>
</div>

<h4>Need to create a account?</h4>
<%= link_to "Signup", signup_path %>